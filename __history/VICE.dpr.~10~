program VICE;

{$APPTYPE CONSOLE}

{$R *.res}

uses
  System.SysUtils,
  vicelib in 'vicelib.pas';

begin
  try
    { TODO -oUser -cConsole Main : Insert code here }
    pixel(1);
    pixel(2);
    handle();
  except
    on E: Exception do
      Writeln(E.ClassName, ': ', E.Message);
  end;
end.
